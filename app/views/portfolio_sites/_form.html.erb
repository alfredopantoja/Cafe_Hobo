<%= form_for(@portfolio_site) do |f| %>
  <% if @portfolio_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@portfolio_site.errors.count, "error") %> prohibited this portfolio_site from being saved:</h2>

      <ul>
      <% @portfolio_site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :screenshot_url %><br />
    <%= f.text_field :screenshot_url %>
  </div>
  <div class="field">
    <%= f.label :site_url %><br />
    <%= f.text_field :site_url %>
  </div>
  <div class="field">
    <%= f.label :order %><br />
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
